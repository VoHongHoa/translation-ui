src/
├── components/     # Components dùng chung (Button, Input,...)
├── features/       # Quản lý theo tính năng (Auth, Dashboard,...)
├── hooks/          # Custom hooks dùng chung
├── layouts/        # Layouts của ứng dụng (MainLayout, AuthLayout)
├── routes/         # Cấu hình Routing
├── services/       # Cấu hình API client (Axios, Fetch)
├── store/          # Zustand global stores
├── types/          # TypeScript types/interfaces toàn cục
├── App.tsx
└── main.tsx